<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/splash_bg"
    android:padding="40dp">

    <ImageView
        android:id="@+id/iv_logo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/ic_logo"
        android:contentDescription="SheetLab Logo"/>

    <TextView
        android:id="@+id/tv_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SheetLab"
        android:textSize="40sp"
        android:textStyle="bold"
        android:textColor="@color/splash_text"
        android:layout_marginTop="20dp"
        android:letterSpacing="0.1"/>

    <View
        android:id="@+id/glow_line"
        android:layout_width="200dp"
        android:layout_height="2dp"
        android:background="@color/accent"
        android:layout_marginTop="12dp"/>

    <TextView
        android:id="@+id/tv_tagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your pocket spreadsheet"
        android:textSize="14sp"
        android:textColor="@color/splash_sub"
        android:layout_marginTop="10dp"/>

    <TextView
        android:id="@+id/tv_owner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Created by axSaaFe"
        android:textSize="12sp"
        android:textColor="@color/splash_owner"
        android:layout_marginTop="8dp"/>

</LinearLayout>
